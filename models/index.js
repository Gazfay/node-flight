var airlinesModel = require('./airlinesModel');
var airportsModel = require('./airportsModel');
var flightSearchModel = require('./flightSearchModel');

var models = {
  airlinesModel: airlinesModel,
  airportsModel: airportsModel,
  flightSearchModel: flightSearchModel
}

module.exports = models;